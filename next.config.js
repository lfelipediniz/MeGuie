/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["i.ytimg.com", "example.com"], // Adicione todos os domínios que você for usar
  },
};

module.exports = nextConfig;

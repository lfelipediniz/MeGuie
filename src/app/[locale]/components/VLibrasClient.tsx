// components/VLibrasClient.tsx
"use client";

import VLibras from "vlibras-nextjs";

const VLibrasClient = () => {
  return <VLibras forceOnload aria-label="VLibras Widget" />;
};

export default VLibrasClient;
